<div id="clients">
    <video id="broadcast" autoplay style="display:block;float:left;"></video>
    <div class="buttons">
        <button class="filter" value="none"/>None</button>
        <button class="filter" value="blur"/>Blur</button>
        <button class="filter" value="gray"/>Grey</button>
        <button class="filter" value="sepia"/>Sepia</button>
        <button class="filter" value="bright"/>Bright</button>
        <button class="filter" value="highcontrast"/>Contrast</button>
        <button class="filter" value="colour"/>Colourful</button>
        <button class="filter" value="invert"/>Invert</button>
        <button class="filter" value="saturate"/>Saturate</button>
    </div>
    <canvas id="canvas" height="300" width="400"></canvas>
</div>
<br/>
<div id="status"></div>
<script src="http://<?php echo $_SERVER['HTTP_HOST']?>:1337/socket.io/socket.io.js"></script>
<script src="/js/video.js"></script>   
<script src="/js/broadcast.js"></script>
<script>
$(function() {
    var elem = document.getElementById("broadcast");
    addfullscreen(elem);
    $('.filter').click(function() {
        $('#broadcast').attr('class', $(this).attr('value'));
    });
});
</script>
<style>
    .buttons {
        width: 15px;
        height: 300px;
        float:left;
        display:block;
    }
    #broadcast {
        width: 400px;
        height: 300px;
        border: outset;
    }
    #canvas {
        display:none;
        width: 400px;
        height: 300px;
        border: outset;
    }
    .clientvideo {
        width: 400px;
        height: 300px;
        border: outset;
    }
    .none {
        -webkit-filter: none;
    }
    .blur {
        -webkit-filter: blur(3px);
    }
    .gray {
        -webkit-filter: grayscale(1);
    }
    .sepia {
        -webkit-filter: sepia(1);
    }
    .bright {
        -webkit-filter: brightness(0.5);
    }
    .highcontrast {
        -webkit-filter: contrast(5);
    }
    .colour {
        -webkit-filter: hue-rotate(180deg);
    }
    .invert {
        -webkit-filter: invert(1);
    }
    .saturate {
        -webkit-filter: saturate(5);
    }
</style>
