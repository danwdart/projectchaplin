To install the dependencies of Project Chaplin, you will need to install the
following packages:

Apache + PHP
MongoDB + phpmongo
Redis + phpredis

A script to install these on Ubuntu is provided (install_ubuntu.sh).
